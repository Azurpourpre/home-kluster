namespace: grafana
configMapGenerator:
  - name: grafana-values
    files: 
    - values.yaml=grafana.values.yaml
  - name: loki-values
    files:
    - values.yaml=loki.values.yaml
generatorOptions:
  disableNameSuffixHash: true