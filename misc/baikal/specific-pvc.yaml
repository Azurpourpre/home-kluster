apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-specific-pvc
  namespace: baikal
spec:
  storageClassName: longhorn-xfs
  accessModes:
    - ReadWriteOnce  
  resources: 
    requests:
      storage: 5Gi