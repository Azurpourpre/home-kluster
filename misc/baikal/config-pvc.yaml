apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-config-pvc
  namespace: baikal
spec:
  storageClassName: longhorn-xfs
  accessModes:
    - ReadWriteOnce  
  resources: 
    requests:
      storage: 1Gi