apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freshrss-pvc
  namespace: freshrss
spec:
  storageClassName: longhorn-xfs
  accessModes:
    - ReadWriteOnce  
  resources: 
    requests:
      storage: 3Gi